{
  "name": "vocabbreak",
  "version": "1.0.0",
  "main": "background.js",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && echo \"Extension built! Load it in Chrome at chrome://extensions/\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@supabase/supabase-js": "^2.56.0"
  }
}
