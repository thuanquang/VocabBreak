{
  "name": "vocabbreak",
  "version": "1.0.1",
  "main": "background.js",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "npm run build && echo \"Extension built in ./dist! Load it in Chrome at chrome://extensions/\"",
    "copy:supabase": "node scripts/copy-supabase.js",
    "spec:validate": "openspec validate --strict",
    "test": "npm run spec:validate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@supabase/supabase-js": "^2.56.0"
  }
}
